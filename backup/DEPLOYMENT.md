# 生產部署指南

## 最新更新 (v1.3.8 - 2025-07-08)

### 部署前檢查清單
**新增檢查項目**：
- [ ] 確認交易統計格式優化功能正常運作（開倉價格、平倉價格、單筆損益）
- [ ] 測試單筆損益計算功能（簡單配對邏輯）
- [ ] 驗證點值系統正確性（大台200、小台50、微台10）
- [ ] 檢查數字格式化改善效果（去除.0後綴）
- [ ] 測試日誌時間戳格式改善（支援新舊格式）
- [ ] 驗證 port.txt 預設值修正（背景執行模式）
- [ ] 檢查API連線狀態訊息修正效果

### 重要部署注意事項
1. **交易統計格式優化**：新增完整的交易明細顯示，包含開倉價格、平倉價格和單筆損益
2. **單筆損益計算**：實作簡單配對邏輯，計算每筆平倉交易的損益
3. **點值系統**：正確設定各合約點值（大台200、小台50、微台10）
4. **數字格式化改善**：修復數字格式化問題，去除不必要的 `.0` 後綴
5. **日誌時間戳格式改善**：改善日誌時間戳格式和API連線狀態顯示
6. **port.txt 預設值修正**：修正預設值為背景執行模式

### 升級建議
- **測試環境驗證**：建議在測試環境先驗證v1.3.8的新功能
- **交易統計測試**：重點測試交易統計的格式和損益計算功能
- **數字格式化驗證**：確認數字顯示不再有 `.0` 後綴
- **日誌格式測試**：驗證日誌時間戳的新格式和排序功能
- **API狀態檢查**：確認API連線狀態訊息的正確顯示

### 新增功能說明
- **交易明細格式**：`大台｜多單｜1口｜23,100｜23,200`（左邊開倉價格，右邊平倉價格）
- **單筆損益顯示**：`＄20,000 TWD`（每筆平倉交易的損益）
- **損益計算邏輯**：平多倉 `(平倉價 - 開倉價) × 數量 × 點值`，平空倉 `(開倉價 - 平倉價) × 數量 × 點值`
- **系統分工**：系統負責統計分析，永豐API負責正式損益計算

## 最新更新 (v1.3.7 - 2025-07-07)

### 部署前檢查清單
**新增檢查項目**：
- [ ] 確認後端日誌顯示開關功能正常運作（`log_console` 設定）
- [ ] 測試背景執行模式下的視窗隱藏功能
- [ ] 驗證程式完全退出機制（關閉前端視窗時完全退出）
- [ ] 檢查 `port.txt` 設定檔案格式正確性
- [ ] 測試啟動時顯示的端口和日誌模式設定

### 重要部署注意事項
1. **後端日誌顯示開關**：新增 `log_console` 設定，支援背景執行模式
2. **程式完全退出機制**：關閉前端視窗時確保後端程式完全退出，避免背景殘留
3. **視窗隱藏機制**：使用 Windows API 隱藏命令行視窗
4. **設定透明化**：程式啟動時顯示當前端口和日誌模式設定

### 升級建議
- **測試環境驗證**：建議在測試環境先驗證v1.3.7的新功能
- **背景執行測試**：重點測試 `log_console:0` 背景執行模式
- **程式退出驗證**：確認關閉前端視窗時程式完全退出
- **設定檔案測試**：驗證 `port.txt` 的新格式和參數

### 新增設定檔案格式
```txt
port:5000
log_console:1
```
- `port`: 系統端口號（1024-65535）
- `log_console`: 日誌顯示模式（0=背景執行，1=正常顯示）

## 最新更新 (v1.3.6 - 2025-07-06)
- 轉倉/保證金前端日誌、日誌格式優化、顏色說明、細節修正。

## 最新更新 (v1.3.5 - 2025-07-05)

### 部署前檢查清單
**新增檢查項目**：
- [ ] 確認交易記錄持久化系統正常運作（`server/transdata/` 目錄）
- [ ] 測試通知一致性修復效果（手動平倉通知）
- [ ] 驗證訂單映射增強功能（智能推斷邏輯）
- [ ] 檢查自動清理機制（30天交易記錄保留）
- [ ] 測試動作顯示統一函數（`get_action_display_by_rule()`）

### 重要部署注意事項
1. **交易記錄持久化**：新增 JSON 檔案儲存機制，確保所有交易參數完整保存
2. **通知一致性修復**：解決手動平倉通知顯示錯誤的問題，確保提交和成交通知內容一致
3. **訂單映射增強**：改進 `order_callback` 函數的推斷邏輯，提升通知準確性
4. **自動清理機制**：自動保留最近 30 天的交易記錄，避免磁碟空間浪費

### 升級建議
- **測試環境驗證**：建議在測試環境先驗證v1.3.5的新功能
- **交易記錄測試**：重點測試交易記錄的保存和讀取功能
- **通知一致性驗證**：確認手動平倉的提交通知和成交通知內容一致
- **清理機制測試**：驗證自動清理舊交易記錄的功能

### 新增檔案結構
```
server/
├── transdata/               # 新增：交易記錄目錄
│   ├── trades_20250705.json # 每日交易記錄檔案
│   └── ...                  # 其他日期檔案
└── ...                      # 其他現有檔案
```

## 最新更新 (v1.3.4 - 2025-07-04)

### 部署前檢查清單
**新增檢查項目**：
- [ ] 確認永豐手動下單參數格式標準化（使用官方參數）
- [ ] 測試手動/自動下單邏輯分離功能
- [ ] 驗證開平倉邏輯優化（移除預設為開倉）
- [ ] 檢查平倉成交通知修復效果
- [ ] 測試動作顯示標準化功能

### 重要部署注意事項
1. **重大架構變更**：v1.3.4手動下單改為使用永豐官方參數格式，需要更新前端調用方式
2. **參數格式標準化**：手動下單必須提供 `action` (Buy/Sell) 和 `octype` (New/Cover) 參數
3. **開平倉邏輯優化**：移除預設為開倉的邏輯，確保開平倉類型準確判斷
4. **通知準確性提升**：修復平倉成交通知顯示錯誤，確保通知訊息準確性

### 升級建議
- **測試環境驗證**：建議在測試環境先驗證v1.3.4的新功能
- **通知功能測試**：重點測試手動平倉和訂單取消的通知格式
- **錯誤處理驗證**：確認各種錯誤情況下的訊息翻譯是否正確

## 系統要求

1. **作業系統**：建議使用 Linux 或 Windows Server 部署
2. **Python**：Python 3.8+ 與 pip
3. **網路**：穩定的網路連線，支援 ngrok 服務
4. **硬碟空間**：至少 1GB 可用空間

## 部署流程

### 1. 環境準備
```bash
# 安裝 Python 依賴
pip install -r requirements.txt

# 確保 ngrok.exe 存在於 server/ 目錄
# 程式會自動檢查並下載最新版本
```

### 2. 設定配置
```bash
# 設定環境變數與 .env 檔案
# 包含永豐 API 憑證、Telegram Bot 設定等
```

### 3. 啟動腳本設定
```bash
# Linux systemd 服務
sudo systemctl enable autotx
sudo systemctl start autotx

# Windows 服務
# 使用 NSSM 或 Task Scheduler 設定開機自啟
```

## ngrok 自動管理

### 重要說明
- **無需手動管理**：ngrok 由程式自動啟動、停止、重啟
- **自動啟動**：程式啟動時自動啟動 ngrok 背景進程
- **自動重啟**：檢測到 ngrok 異常時自動重啟（5秒延遲）
- **自動關閉**：程式退出時自動清理 ngrok 進程
- **自動升級**：背景檢查並支援 ngrok 版本自動更新

### 部署注意事項
1. **防火牆設定**：確保 5002 端口可被 ngrok 訪問
2. **網路穩定性**：ngrok 需要穩定的網路連線
3. **進程管理**：程式會自動管理 ngrok 進程，無需手動干預
4. **資源清理**：程式退出時會自動清理所有資源

### 監控建議
- 定期檢查 `shioaji.log` 中的錯誤訊息
- 監控 ngrok 狀態 API：`/api/ngrok/status`
- 檢查前端系統日誌和 ngrok 請求日誌
- 監控系統資源使用情況

## 備份策略

### 重要檔案備份
- `backup/` 資料夾與所有文檔
- `server/config/.env` 設定檔
- `server/holiday/` 假期檔案
- `server/certificate/` 憑證檔案
- `shioaji.log` 交易日誌

### 定期備份
- 建議每日備份重要設定檔
- 每週備份完整系統
- 重要升級前進行完整備份

## 升級流程

### 重要升級注意事項
1. **測試環境**：重要升級請先測試於測試環境
2. **備份**：升級前進行完整備份
3. **ngrok 管理**：升級後 ngrok 會自動重新啟動
4. **版本檢查**：程式會自動檢查並更新 ngrok 版本

### 升級步驟
```bash
# 1. 備份現有系統
cp -r AutoTX AutoTX_backup_$(date +%Y%m%d)

# 2. 更新程式碼
git pull origin main

# 3. 重啟服務
sudo systemctl restart autotx

# 4. 檢查狀態
curl http://localhost:5002/api/ngrok/status
```

## 故障排除

### 常見部署問題
- **ngrok 無法啟動**：檢查網路連線和防火牆設定
- **API 連線失敗**：檢查永豐 API 憑證和網路
- **進程殘留**：確保程式正常退出，檢查 `cleanup_on_exit()` 執行

### 緊急處理
- 程式異常時會自動重啟 ngrok
- 如需手動重啟，使用 `/api/ngrok/restart` API
- 嚴重問題可重啟整個系統

---

詳細部署流程請參考 README.md 與 QUICK_START.md 