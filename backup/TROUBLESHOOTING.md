# 故障排除指南

## 最新重大更新 (v1.4.2 - 2025-07-16)

### TX合約選擇與持倉狀態顯示問題

#### 問題：TX交易面板顯示錯誤合約
**症狀**：
- TX交易面板顯示G5合約而非R1合約
- 非轉倉模式下選擇錯誤的合約
- 前端合約選擇與預期不符

**解決方案**：
1. **檢查合約選擇邏輯**：確認非轉倉模式下優先尋找R1合約
2. **清除快取**：清除瀏覽器快取並重新載入頁面
3. **檢查合約資料**：確認永豐API返回包含R1後綴的合約
4. **重新整理合約**：使用前端重新整理按鈕更新合約資訊

#### 問題：持倉狀態所有欄位顯示"-"
**症狀**：
- 持倉狀態中動作、數量、均價、市價、未實現損益都顯示"-"
- 明確持有部位但狀態不顯示
- 後端API返回錯誤或null值

**解決方案**：
1. **檢查API連線**：確認永豐API正常連線並登入
2. **檢查持倉API**：驗證/api/position/status端點正常運作
3. **檢查null處理**：確認後端正確處理position.pnl為null的情況
4. **重新登入**：嘗試重新登入永豐API以刷新連線狀態

#### 問題：個別損益顯示"-"而非數字
**症狀**：
- 持倉狀態中未實現損益欄位顯示"-"
- 其他欄位正常但損益無法顯示
- 前端JavaScript解析錯誤

**解決方案**：
1. **檢查欄位名稱**：確認前端使用正確欄位名稱"未實現損益"
2. **檢查API回傳**：驗證後端API返回正確的損益數值
3. **檢查null處理**：確認position.pnl不為null或undefined
4. **瀏覽器控制台**：檢查JavaScript錯誤訊息

#### 問題：前端欄位映射錯誤
**症狀**：
- 前端無法正確讀取後端資料
- 欄位名稱不匹配導致undefined
- 資料解析失敗

**解決方案**：
1. **檢查欄位名稱**：確認前後端使用相同欄位名稱
2. **API測試**：直接測試API端點返回的資料結構
3. **JavaScript除錯**：使用console.log檢查資料流程
4. **網路面板**：檢查瀏覽器開發者工具的網路請求

---

## 版本歷史問題 (v1.4.1 - 2025-07-15)

### 轉倉系統完整重構問題

#### 問題：轉倉模式未正確觸發
**症狀**：
- 已經到達交割日前一天但系統未進入轉倉模式
- rollover_mode仍為False
- 前端仍顯示R1當月合約而非R2次月合約

**解決方案**：
1. **檢查交割日計算**：確認get_third_wednesday函數正確計算第三個星期三
2. **檢查觸發條件**：驗證當前日期 >= 交割日前一天的比較邏輯
3. **手動觸發**：可使用API手動檢查轉倉狀態
4. **重新啟動**：系統重啟時會自動檢查轉倉狀態

#### 問題：前端顯示錯誤的合約
**症狀**：
- 轉倉期間前端仍顯示R1合約
- API返回的合約信息不正確
- 下單使用錯誤的合約

**解決方案**：
1. **檢查API端點**：確認api_futures_contracts使用rollover_mode邏輯
2. **檢查R2合約檢測**：驗證get_next_month_contracts函數能找到R2合約
3. **清除快取**：清除瀏覽器快取並重新載入
4. **檢查合約排序**：確認合約按交割日期正確排序

#### 問題：轉倉通知重複發送
**症狀**：
- 系統重啟後重複發送轉倉通知
- 收到多條相同的轉倉通知
- 通知時序混亂

**解決方案**：
1. **檢查通知機制**：確認rollover_notified狀態正確管理
2. **檢查觸發條件**：只有首次進入轉倉模式才發送通知
3. **重新啟動**：系統重啟時應不重複發送通知
4. **檢查通知時序**：確認通知分離時間（提交2秒、成交5秒）

#### 問題：R2合約檢測失敗
**症狀**：
- 無法找到R2後綴的合約
- 系統使用第二個合約作為回退
- 合約選擇邏輯不正確

**解決方案**：
1. **檢查合約資料**：確認永豐API返回的合約包含R2後綴
2. **檢查檢測邏輯**：驗證contract.code.endswith('R2')邏輯
3. **檢查回退機制**：確認使用第二個合約作為次月合約
4. **手動測試**：使用API測試合約選擇邏輯

#### 問題：Excel報表顏色格式不統一
**症狀**：
- TX和BTC報表顏色格式不一致
- 部分報表仍使用舊的顏色格式
- blue_fill或gray_fill變數未定義錯誤

**解決方案**：
1. **檢查顏色定義**：確認blue_fill(#B8CCE4)和gray_fill(#D9D9D9)正確定義
2. **檢查報表函數**：驗證所有報表生成函數使用統一顏色
3. **檢查欄寬設置**：確認A-K欄寬統一設為19
4. **重新生成**：刪除舊報表文件，重新生成新格式報表

#### 問題：系統日誌排序錯誤
**症狀**：
- 日誌跨日期邊界時排序混亂
- 昨天的日誌顯示在今天前面
- 時間戳解析錯誤

**解決方案**：
1. **檢查排序邏輯**：確認使用datetime對象比較而非字符串比較
2. **檢查時間格式**：驗證智能時間戳解析支援各種格式
3. **檢查時區處理**：確認CST時區標識正確處理
4. **重新載入**：重新載入系統日誌頁面

#### 問題：代碼清理後功能異常
**症狀**：
- 移除測試代碼後出現未定義錯誤
- 某些功能無法正常運作
- 調試信息過多或過少

**解決方案**：
1. **檢查關鍵函數**：確認重要功能函數未被誤刪
2. **檢查變數定義**：確認必要變數和導入語句完整
3. **檢查語法錯誤**：使用python -m py_compile檢查語法
4. **保留必要日誌**：確認print_console轉倉狀態日誌保留

#### 問題：Telegram通知時序問題
**症狀**：
- 提交和成交通知"黏在一起"
- 通知時間間隔太短
- 通知順序混亂

**解決方案**：
1. **檢查延遲設置**：確認提交通知延遲2秒、成交通知延遲5秒
2. **檢查線程處理**：驗證通知發送使用正確的線程處理
3. **檢查通知內容**：確認通知內容在轉倉期間正確顯示R2合約
4. **測試通知時序**：手動測試通知發送間隔

### 轉倉系統偵錯步驟

**檢查轉倉狀態**：
```python
# 檢查轉倉模式
print(f"rollover_mode: {rollover_mode}")
print(f"rollover_notified: {rollover_notified}")

# 檢查交割日計算
current_date = datetime.now().date()
third_wednesday = get_third_wednesday(current_date.year, current_date.month)
rollover_start = third_wednesday - timedelta(days=1)
print(f"交割日: {third_wednesday}")
print(f"轉倉開始日: {rollover_start}")
print(f"當前日期: {current_date}")
```

**檢查R2合約**：
```python
# 檢查R2合約檢測
contracts = api.Contracts.Futures.TXF
sorted_contracts = sorted(contracts, key=lambda x: x.delivery_date)
for contract in sorted_contracts:
    print(f"合約: {contract.code}, 交割日: {contract.delivery_date}")
    if contract.code.endswith('R2'):
        print(f"找到R2合約: {contract.code}")
```

**檢查API端點**：
```bash
# 測試合約選擇API
curl http://localhost:5000/api/futures/contracts

# 測試轉倉狀態API
curl http://localhost:5000/api/futures/rollover_status
```

---

## 歷史更新 (v1.4.0 - 2025-07-14)

### BTC加密貨幣交易系統整合問題

#### 問題：BTC模組導入失敗
**症狀**：
- 系統啟動時出現 `ModuleNotFoundError: No module named 'btcmain'`
- BTC面板顯示「BTC模組不可用」

**解決方案**：
1. **檢查文件**：確認 `btcmain.py` 文件存在於 server 目錄
2. **檢查語法**：運行 `python -m py_compile btcmain.py` 檢查語法錯誤
3. **重新啟動**：完全關閉後重新啟動系統
4. **檢查路徑**：確認 Python 能找到模組文件

#### 問題：BTC API連線失敗
**症狀**：
- BTC登入失敗，顯示API錯誤
- 幣安API返回簽名錯誤

**解決方案**：
1. **檢查配置**：確認 `config/btc.env` 中API金鑰正確
2. **檢查權限**：確認幣安API金鑰有期貨交易權限
3. **檢查時間**：確保系統時間與UTC時間同步
4. **檢查網路**：確認能正常訪問 fapi.binance.com

#### 問題：Webhook訊號路由錯誤
**症狀**：
- TX訊號被錯誤路由到BTC系統
- BTC訊號被錯誤路由到TX系統

**解決方案**：
1. **檢查訊號格式**：確認TradingView發送的JSON格式正確
2. **檢查識別邏輯**：
   - TX訊號應包含 `tradeId`, `contract` 字段
   - BTC訊號應包含 `symbol`, `action` 字段
3. **使用明確路由**：
   - TX系統：`/webhook/tx`
   - BTC系統：`/webhook/btc`

#### 問題：雙系統日誌混亂
**症狀**：
- TX和BTC日誌顯示在錯誤面板
- 系統日誌無法正確分離

**解決方案**：
1. **清除快取**：清除瀏覽器快取並重新整理
2. **檢查API**：確認 `/api/system_log` 正確處理不同系統日誌
3. **重新載入**：重新載入前端頁面

#### 問題：BTCtransdata目錄權限錯誤
**症狀**：
- BTC交易記錄無法保存
- 報表生成失敗

**解決方案**：
1. **檢查目錄**：確認 `BTCtransdata` 目錄存在
2. **檢查權限**：確保Python進程有寫入權限
3. **手動創建**：如需要手動創建目錄：`mkdir BTCtransdata`

#### 問題：配置文件載入錯誤
**症狀**：
- BTC系統無法載入配置
- 環境變數讀取失敗

**解決方案**：
1. **檢查格式**：確認 `config/btc.env` 格式正確（KEY=VALUE）
2. **檢查編碼**：確保文件使用UTF-8編碼
3. **檢查必要字段**：確認所有必要配置項目都存在

**必要配置檢查清單**：
```
BOT_TOKEN_BTC=xxx
CHAT_ID_BTC=xxx  
BINANCE_API_KEY=xxx
BINANCE_SECRET_KEY=xxx
TRADING_PAIR=BTCUSDT
LEVERAGE=5
LOGIN_BTC=1
```

#### 問題：代碼清理後功能異常
**症狀**：
- 原有功能無法正常運作
- 出現未定義的變數或函數錯誤

**解決方案**：
1. **檢查import**：確認所有必要的import都存在
2. **檢查函數**：確認函數名稱和參數正確
3. **查看日誌**：檢查後端日誌中的具體錯誤訊息
4. **重新部署**：如果問題持續，重新部署整個系統

### 通用偵錯步驟

**系統啟動問題**：
1. 檢查Python語法：`python -m py_compile main.py`
2. 檢查模組導入：`python -c "import btcmain; print('BTC模組正常')"`
3. 檢查配置文件：確認所有.env文件格式正確
4. 檢查目錄權限：確保所有必要目錄存在且可寫

**Webhook測試**：
1. 使用curl測試TX訊號：
```bash
curl -X POST http://localhost:5000/webhook \
  -H "Content-Type: application/json" \
  -d '{"tradeId":"test","action":"buy","contract":"TXFR5"}'
```

2. 使用curl測試BTC訊號：
```bash
curl -X POST http://localhost:5000/webhook \
  -H "Content-Type: application/json" \
  -d '{"action":"LONG","symbol":"BTCUSDT","price":50000}'
```

## 歷史更新 (v1.3.11 - 2025-07-12)

### 隧道服務重構與界面優化問題
**隧道服務重構相關問題**：修復隧道模組重命名和域名選項更新

#### 問題：隧道模組導入失敗
**症狀**：
- 系統啟動時出現 `ModuleNotFoundError: No module named 'cloudflare_tunnel'`
- 隧道服務無法啟動

**解決方案**：
1. **已修復**：v1.3.11版本已將模組重命名為 `tunnel.py`
2. **檢查導入**：確認代碼使用 `from tunnel import CloudflareTunnel`
3. **清除快取**：刪除 `__pycache__` 資料夾重新啟動

#### 問題：Cloudflare Tunnel 記錄失敗
**症狀**：
- 錯誤訊息：`name 'format_timestamp' is not defined`
- Cloudflare Tunnel 日誌無法正常記錄

**解決方案**：
1. **已修復**：v1.3.11版本已新增 `format_timestamp` 函數
2. **檢查函數**：確認函數在 `main.py` 中正確定義
3. **重新啟動**：重啟程式確保新函數載入

**檢查代碼**：
```python
def format_timestamp(timestamp_str):
    if not timestamp_str:
        return ''
    try:
        if 'T' in timestamp_str:
            dt = datetime.fromisoformat(timestamp_str.replace('Z', '+00:00'))
        else:
            dt = datetime.strptime(timestamp_str, '%Y-%m-%d %H:%M:%S')
        local_dt = dt.replace(tzinfo=timezone.utc).astimezone()
        return local_dt.strftime('%H:%M:%S')
    except:
        return timestamp_str[-8:] if len(timestamp_str) >= 8 else timestamp_str
```

#### 問題：域名選項顯示錯誤
**症狀**：
- 前端仍顯示 workers.dev 域名選項
- 用戶選擇 workers 模式但無法使用

**解決方案**：
1. **已修復**：v1.3.11版本已移除 workers.dev 選項
2. **清除快取**：清除瀏覽器快取確保界面更新
3. **檢查前端**：確認 HTML 選項已更新

#### 問題：狀態文字對齊混亂
**症狀**：
- online/offline/checking 狀態文字不在同一水平線
- 界面顯示不美觀

**解決方案**：
1. **已修復**：v1.3.11版本已修正 CSS 對齊
2. **檢查樣式**：確認以下 CSS 生效
```css
#ngrok-status, #sinopac-api-status, #tunnel-status {
    display: flex !important;
    align-items: flex-end !important;
    line-height: 1 !important;
    min-height: 24px;
}
```
3. **強制刷新**：使用 Ctrl+F5 強制刷新頁面

#### 問題：請求計數顯示格式不一致
**症狀**：
- 某些地方顯示 "5 筆"，某些地方顯示 "5"
- 格式不統一影響美觀

**解決方案**：
1. **已修復**：v1.3.11版本已統一移除「筆」字
2. **檢查樣式**：確認 `.requests-count` CSS 類正確應用
3. **驗證顯示**：確認所有請求計數都顯示純數字

#### 問題：版本日誌重複顯示
**症狀**：
- 啟動時重複顯示永豐版本日誌
- 日誌記錄冗餘

**解決方案**：
1. **已修復**：v1.3.11版本已移除初啟動版本日誌
2. **檢查邏輯**：確認 `getSinopacVersion()` 函數不記錄初始日誌
3. **只保留更新**：只有版本更新時才記錄日誌

#### 問題：xlsx 生成無前端日誌
**症狀**：
- xlsx 文件成功生成但前端無日誌記錄
- 用戶無法確認文件是否生成成功

**解決方案**：
1. **已修復**：v1.3.11版本已新增生成成功日誌
2. **檢查流程**：確認生成流程包含前端日誌記錄
3. **驗證通知**：確認成功後有對應的前端通知

#### 問題：TG 通知日誌重複
**症狀**：
- 收到多條相同的 TG 發送成功日誌
- 日誌記錄冗餘混亂

**解決方案**：
1. **已修復**：v1.3.11版本已移除重複的 TG 通知日誌
2. **保留必要**：只保留文件生成成功日誌
3. **簡化流程**：避免不必要的重複通知

#### 問題：週六夜盤統計異常
**症狀**：
- 週六夜盤時段交易統計未正確執行
- 報表生成時間不正確

**解決方案**：
1. **檢查邏輯**：確認週六視為交易日的邏輯正確
2. **時間判斷**：週六凌晨05:00前視為交易時間
3. **統計排程**：確認排程正確執行

**調試步驟**：
```python
# 檢查交易日判斷
from datetime import datetime
def debug_trading_day():
    now = datetime.now()
    is_trading = is_trading_day_advanced(now)
    print(f"當前時間：{now}")
    print(f"是否為交易日：{is_trading}")
    if now.weekday() == 5:  # 週六
        print(f"週六夜盤判斷：{now.hour < 5}")
```

### 界面顯示優化驗證清單
- [ ] 隧道模組導入正常，無 ModuleNotFoundError
- [ ] 時間戳處理函數正常，無 NameError
- [ ] 域名選項正確，無 workers.dev 選項
- [ ] 狀態文字對齊美觀，垂直靠下對齊
- [ ] 請求計數格式統一，純數字顯示
- [ ] 版本日誌不重複，只有更新時記錄
- [ ] xlsx 生成有日誌，前端可見成功訊息
- [ ] TG 通知簡化，無重複日誌記錄
- [ ] 週六夜盤統計正常，報表按時生成

---

## 版本歷史：v1.3.10 (2025-07-10)

### 日誌顯示邏輯修正與格式優化問題
**重大修復**：修正平倉交易的方向顯示錯誤問題和價格格式統一

#### 問題：平倉交易方向顯示錯誤
**症狀**：
- 平空單顯示為「多單」，實際應該顯示為「空單」
- 平多單顯示為「空單」，實際應該顯示為「多單」
- 交易日誌讓人誤解實際的交易方向

**解決方案**：
1. **已修復**：v1.3.10版本已修正方向顯示邏輯
2. **檢查更新**：確認使用最新版本的 `get_simple_order_log_message()` 函數
3. **測試案例**：
   - 手動平空單 (BUY + COVER) → 應顯示「手動平倉：大台｜空單｜...」
   - 手動平多單 (SELL + COVER) → 應顯示「手動平倉：大台｜多單｜...」

**故障排查步驟**：
```python
# 檢查方向顯示邏輯
def debug_direction_display(octype, direction):
    if str(octype).upper() == 'NEW':
        result = '多單' if direction.upper() == 'BUY' else '空單'
    elif str(octype).upper() == 'COVER':
        result = '空單' if direction.upper() == 'BUY' else '多單'  # 關鍵邏輯
    print(f"octype={octype}, direction={direction} → {result}")
    return result
```

#### 問題：價格顯示格式不一致
**症狀**：
- 某些地方顯示 `$ 22,000`，某些地方顯示 `22,000`
- 保證金通知顯示 `大台$184,000`
- 統計報表顯示 `＄20,000 TWD`

**解決方案**：
1. **已修復**：v1.3.10版本已統一移除所有「$」符號
2. **檢查所有格式化函數**：確認以下函數已更新
   - `get_simple_order_log_message()`
   - `send_margin_change_notification()`
   - `generate_trading_report()`
3. **清除快取**：重新啟動程式確保所有修改生效

**檢查清單**：
- [ ] 日誌訊息不再包含 `$` 符號
- [ ] 保證金通知格式為 `大台 184,000`
- [ ] 統計報表格式為 `20,000 TWD`
- [ ] 千分位逗號分隔仍然保留

#### 問題：Webhook 回調流程不完整
**症狀**：
- 只收到部分 webhook 相關日誌
- 無法追蹤完整的交易生命週期
- 訊號接收後沒有後續日誌

**解決方案**：
1. **檢查回調流程**：確認三個步驟都正常執行
   - 步驟1：`來自 webhook開倉訊號：開多`
   - 步驟2：`自動開倉：大台｜多單｜1 口｜22,000｜限價 (ROD)`
   - 步驟3：`開倉成功：大台｜多單｜1 口｜22,000｜限價 (ROD)`
2. **檢查前端日誌API**：確認 `/api/system_log` 端點正常響應
3. **檢查網路連線**：確認內部API調用正常

**故障排查步驟**：
```python
# 測試前端日誌API
import requests
response = requests.post(
    'http://127.0.0.1:5000/api/system_log',
    json={'message': '測試訊息', 'type': 'info'},
    timeout=5
)
print(f"API回應：{response.status_code}")
```

#### 問題：備援邏輯被觸發
**症狀**：
- 日誌中出現使用備援邏輯的情況
- 方向顯示可能不正確

**解決方案**：
1. **檢查參數傳遞**：確認 `octype` 參數正確傳遞
2. **檢查參數值**：確認 `octype` 為 'NEW' 或 'COVER'
3. **加入調試日誌**：暫時加入調試輸出確認參數值

**調試代碼**：
```python
print(f"DEBUG: octype={octype}, type={type(octype)}")
print(f"DEBUG: direction={direction}, type={type(direction)}")
```

#### 問題：舊版本函數仍在使用
**症狀**：
- 更新後仍然出現舊的格式問題
- 方向顯示邏輯沒有改變

**解決方案**：
1. **重新啟動程式**：確保所有修改都已載入
2. **檢查函數版本**：確認使用的是最新版本的函數
3. **清除Python快取**：刪除 `__pycache__` 資料夾

#### 問題：測試環境與生產環境不一致
**症狀**：
- 測試時方向顯示正確，實際使用時仍有問題
- 不同交易場景結果不一致

**解決方案**：
1. **完整測試案例**：執行所有組合的測試
   - NEW + BUY = 多單
   - NEW + SELL = 空單  
   - COVER + BUY = 空單
   - COVER + SELL = 多單
2. **檢查實際參數**：在實際交易時記錄參數值
3. **端到端測試**：從 webhook 到最終日誌的完整測試

**測試腳本**：
```python
def test_all_combinations():
    test_cases = [
        ('NEW', 'BUY', '多單'),
        ('NEW', 'SELL', '空單'),
        ('COVER', 'BUY', '空單'),
        ('COVER', 'SELL', '多單'),
    ]
    
    for octype, direction, expected in test_cases:
        result = get_direction_display(octype, direction)
        status = "✅" if result == expected else "❌"
        print(f"{status} {octype}+{direction} → {result} (期望:{expected})")
```

---

## 最新更新 (v1.3.9 - 2025-07-09)

### 交易月報功能實現問題
**新增功能**：每月最後一個交易日自動生成月報

#### 問題：月末交易日判斷錯誤
**症狀**：
- 非月末交易日也生成月報
- 月末交易日沒有生成月報
- 判斷邏輯不正確

**解決方案**：
1. **檢查判斷邏輯**：確認 `is_last_trading_day_of_month()` 函數正確
2. **檢查交易日API**：確認 `/api/trading/status` 端點正常
3. **手動測試**：在月末交易日手動測試功能

---

## 最新更新 (v1.3.8 - 2025-07-08)

### 交易統計格式優化與損益計算改善問題
**新增功能**：完整的交易明細顯示和單筆損益計算

#### 問題：交易統計中數字顯示 .0 後綴
**症狀**：
- 交易統計中數字顯示為 `1,500,000.0` 而不是 `1,500,000`
- 影響可讀性

**解決方案**：
1. **已修復**：v1.3.8版本已改善數字格式化
2. **檢查格式化函數**：確認 `format_number_for_notification()` 函數正確執行
3. **測試數字顯示**：驗證整數和小數的顯示格式

#### 問題：交易明細缺少開倉價格
**症狀**：
- 交易明細只顯示平倉價格，缺少開倉價格
- 無法計算單筆損益

**解決方案**：
1. **檢查配對邏輯**：確認簡單配對邏輯正確執行
2. **檢查交易記錄**：確認開倉交易記錄完整
3. **檢查合約代碼**：確認開倉和平倉使用相同合約代碼

#### 問題：單筆損益計算錯誤
**症狀**：
- 單筆損益計算結果不正確
- 損益顯示為 0 或負值

**解決方案**：
1. **檢查點值設定**：確認各合約點值正確（大台200、小台50、微台10）
2. **檢查計算公式**：
   - 平多倉：`(平倉價 - 開倉價) × 數量 × 點值`
   - 平空倉：`(開倉價 - 平倉價) × 數量 × 點值`
3. **檢查價格數據**：確認開倉價格和平倉價格正確

#### 問題：日誌時間戳排序混亂
**症狀**：
- 日誌按時間排序時順序錯誤
- 昨夜的日誌跑到前面

**解決方案**：
1. **已修復**：v1.3.8版本已改善時間戳格式
2. **檢查時間格式**：確認使用 `YYYY-MM-DD HH:MM:SS.mmm CST` 格式
3. **檢查解析邏輯**：確認 `parse_time()` 函數支援新舊格式

#### 問題：API連線狀態訊息錯誤
**症狀**：
- "API重新連線成功" 應顯示為 "API連線成功"
- API連線異常訊息顏色不正確

**解決方案**：
1. **已修復**：v1.3.8版本已修正訊息和顏色
2. **檢查訊息邏輯**：確認連線成功和異常的訊息區分
3. **檢查顏色設定**：確認連線異常為橘色（warning），成功為綠色（success）

#### 問題：port.txt 預設值不正確
**症狀**：
- 自動建立的 port.txt 預設為 `log_console:1`（正常顯示）
- 希望預設為背景執行模式

**解決方案**：
1. **已修復**：v1.3.8版本已修正預設值為 `log_console:0`（背景執行）
2. **檢查預設值**：確認所有相關的預設值都改為背景執行
3. **重新建立設定**：刪除 port.txt 讓程式自動重新建立

### 點值系統問題
**新增功能**：正確設定各合約點值

#### 問題：損益計算使用錯誤點值
**症狀**：
- 大台損益計算使用錯誤點值
- 小台或微台損益計算不正確

**解決方案**：
1. **檢查點值設定**：
   - 大台 (TXF)：每點 200 元
   - 小台 (MXF)：每點 50 元
   - 微台 (TMF)：每點 10 元
2. **檢查合約代碼**：確認合約代碼識別正確
3. **測試損益計算**：驗證不同合約的損益計算結果

### 簡單配對邏輯問題
**新增功能**：簡單配對開平倉交易

#### 問題：開倉和平倉無法配對
**症狀**：
- 平倉交易找不到對應的開倉價格
- 單筆損益顯示為 0

**解決方案**：
1. **檢查配對條件**：確認同合約 + 反向動作的配對邏輯
2. **檢查交易記錄**：確認開倉和平倉交易記錄完整
3. **檢查時間順序**：確認使用時間上最早的開倉價格
4. **檢查合約代碼**：確認開倉和平倉使用相同合約代碼

#### 問題：配對邏輯過於複雜
**症狀**：
- 希望使用簡單的配對邏輯
- 不需要複雜的 FIFO 系統

**解決方案**：
1. **已實現**：v1.3.8版本使用簡單配對邏輯
2. **系統分工**：系統負責統計分析，永豐API負責正式損益計算
3. **避免重複**：不重新實作複雜的 FIFO 邏輯

## 最新更新 (v1.3.7 - 2025-07-07)

### 後端日誌顯示開關功能問題
**新增功能**：`log_console` 設定控制後端程式是否在背景執行

#### 問題：背景執行模式下程式無法完全退出
**症狀**：
- 關閉前端視窗後，後端程式仍在背景運行
- 任務管理器中仍有 Python 進程
- 端口仍被占用

**解決方案**：
1. **已修復**：v1.3.7版本已添加 `os._exit(0)` 確保程式完全退出
2. **檢查設定**：確認 `port.txt` 中的 `log_console` 設定正確
3. **手動終止**：如仍有殘留進程，可使用任務管理器手動終止

#### 問題：背景執行模式下命令行視窗無法隱藏
**症狀**：
- 設定 `log_console:0` 後命令行視窗仍然顯示
- 錯誤訊息：`隱藏命令行視窗失敗`

**解決方案**：
1. **檢查權限**：確保程式具有足夠權限隱藏視窗
2. **檢查 Windows API**：確認 `ctypes.windll.user32.ShowWindow` 可用
3. **重新啟動**：重新啟動程式嘗試隱藏視窗

#### 問題：設定檔案格式錯誤
**症狀**：
- 錯誤訊息：`讀取設置失敗`
- 程式使用預設設定啟動

**解決方案**：
1. **檢查檔案格式**：確認 `port.txt` 格式正確
   ```txt
   port:5000
   log_console:1
   ```
2. **檢查參數值**：確保 `log_console` 值為 0 或 1
3. **重新建立**：刪除 `port.txt` 讓程式自動重新建立

#### 問題：啟動時顯示的設定不正確
**症狀**：
- 啟動訊息顯示的端口或日誌模式與實際設定不符
- 設定讀取失敗

**解決方案**：
1. **檢查檔案編碼**：確保 `port.txt` 使用 UTF-8 編碼
2. **檢查檔案權限**：確保檔案具有讀取權限
3. **重新建立設定**：刪除 `port.txt` 讓程式自動重新建立

### 程式退出機制問題
**新增功能**：關閉前端視窗時確保後端程式完全退出

#### 問題：關閉後仍有資源殘留
**症狀**：
- ngrok 進程未被正確關閉
- 永豐 API 連線未被正確登出
- 系統資源未被正確釋放

**解決方案**：
1. **檢查清理函數**：確認 `cleanup_on_exit()` 函數正確執行
2. **檢查進程終止**：確認 `os._exit(0)` 被正確調用
3. **手動清理**：如需要，可手動終止相關進程

## 最新更新 (v1.3.6 - 2025-07-06)
- 轉倉/保證金前端日誌、日誌格式優化、顏色說明、細節修正。

## 最新更新 (v1.3.5 - 2025-07-05)

### 交易記錄持久化系統問題
**新增功能**：交易記錄自動保存到 JSON 檔案，支援從檔案讀取交易參數

#### 問題：交易記錄檔案無法創建
**症狀**：
- 錯誤訊息：`Permission denied` 或 `No such file or directory`
- 交易記錄無法保存

**解決方案**：
1. **檢查目錄權限**：確保 `server/transdata/` 目錄存在且具有寫入權限
2. **創建目錄**：
   ```bash
   mkdir -p server/transdata
   chmod 755 server/transdata
   ```
3. **檢查磁碟空間**：確保有足夠的磁碟空間保存記錄

#### 問題：交易記錄讀取失敗
**症狀**：
- 訂單映射缺失時無法從 JSON 檔案讀取交易參數
- 成交通知顯示不正確的資訊

**解決方案**：
1. **檢查檔案格式**：確認 JSON 檔案格式正確
2. **檢查檔案權限**：確保檔案具有讀取權限
3. **檢查檔案編碼**：確保檔案使用 UTF-8 編碼

#### 問題：自動清理機制異常
**症狀**：
- 舊交易記錄檔案未被清理
- 磁碟空間持續增長

**解決方案**：
1. **檢查清理邏輯**：確認 `cleanup_old_trade_files()` 函數正常執行
2. **手動清理**：
   ```bash
   # 刪除超過30天的檔案
   find server/transdata/ -name "trades_*.json" -mtime +30 -delete
   ```
3. **檢查檔案日期**：確認檔案名格式為 `trades_YYYYMMDD.json`

### 通知一致性修復問題
**重大修復**：解決手動平倉通知顯示錯誤的問題

#### 問題：手動平倉通知顯示錯誤
**症狀**：
- 手動平倉的提交訊息顯示「手動平倉」
- 但成交訊息顯示「手動開倉」
- 動作顯示不一致

**解決方案**：
1. **已修復**：v1.3.5版本已統一通知邏輯
2. **檢查參數來源**：確保提交和成交通知使用相同的參數來源
3. **測試通知一致性**：驗證所有交易通知的準確性

#### 問題：動作顯示不一致
**症狀**：
- 平倉多單顯示為「多單買入」而不是「多單賣出」
- 開倉空單顯示為「空單賣出」而不是「空單買入」

**解決方案**：
1. **使用統一函數**：所有動作顯示都使用 `get_action_display_by_rule()` 函數
2. **檢查邏輯一致性**：
   - `New Buy` = 多單買入
   - `New Sell` = 空單買入
   - `Cover Sell` = 多單賣出
   - `Cover Buy` = 空單賣出

#### 問題：訂單映射缺失處理
**症狀**：
- 當訂單映射缺失時，通知顯示不正確的資訊
- 無法智能推斷開平倉類型

**解決方案**：
1. **改進推斷邏輯**：從持倉資訊智能推斷交易類型
2. **JSON 檔案備用**：支援從交易記錄 JSON 檔案讀取完整參數
3. **測試推斷準確性**：驗證推斷邏輯與實際交易情況的匹配性

### 動作顯示統一函數問題
**新增功能**：`get_action_display_by_rule()` 函數統一動作顯示邏輯

#### 問題：動作顯示邏輯錯誤
**症狀**：
- 動作顯示與實際交易邏輯不匹配
- 開平倉判斷不一致

**解決方案**：
1. **檢查函數邏輯**：確認 `get_action_display_by_rule()` 函數的正確性
2. **測試所有組合**：驗證所有動作組合的顯示邏輯
3. **統一參數來源**：確保所有通知函數都使用相同的參數來源

### 訂單回調推斷邏輯問題
**智能推斷功能**：改進當訂單映射缺失時的推斷邏輯

#### 問題：推斷邏輯不準確
**症狀**：
- 持倉資訊分析錯誤
- 開平倉類型推斷不正確

**解決方案**：
1. **檢查持倉資訊**：確認持倉資訊的準確性
2. **改進推斷邏輯**：根據訂單方向與持倉方向的關係推斷開平倉類型
3. **測試推斷準確性**：驗證推斷邏輯與實際交易情況的匹配性

## 最新更新 (v1.3.4 - 2025-07-04)

### 永豐手動下單參數格式標準化
**重大變更**：手動下單改為使用永豐官方參數格式，與 WEBHOOK 下單明確分離

#### 問題：手動下單缺少必要參數
**症狀**：
- 錯誤訊息：`永豐手動下單缺少官方參數 action 和 octype`
- 下單失敗，無法提交訂單

**解決方案**：
1. **使用正確的參數格式**：
   ```json
   {
     "contract_code": "TXF",
     "quantity": 1,
     "price": 0,
     "action": "Buy",    // 必須提供：Buy 或 Sell
     "octype": "Cover"   // 必須提供：New 或 Cover
   }
   ```
2. **參數對應關係**：
   - 開倉：`"octype": "New"`
   - 平倉：`"octype": "Cover"`
   - 買入：`"action": "Buy"`
   - 賣出：`"action": "Sell"`

#### 問題：平倉成交通知顯示錯誤
**症狀**：
- 平倉成交通知顯示為「手動開倉」而不是「手動平倉」
- 動作顯示錯誤，如平倉多單顯示為「多單買入」

**解決方案**：
1. **已修復**：v1.3.4版本已標準化動作對應邏輯
2. **正確對應**：
   - `New Buy` = 多單買入
   - `New Sell` = 空單買入
   - `Cover Sell` = 多單賣出
   - `Cover Buy` = 空單賣出

#### 問題：開平倉邏輯混淆
**症狀**：
- 手動下單和 WEBHOOK 下單的開平倉邏輯混淆
- 通知訊息中開平倉類型顯示不正確

**解決方案**：
1. **明確分離**：
   - 手動下單：使用 `action` 和 `octype` 參數
   - WEBHOOK 下單：使用中文 `direction` 參數
2. **移除預設邏輯**：不再預設為開倉，確保準確判斷

### 統一失敗通知格式修復
**問題**：手動平倉和訂單取消時顯示錯誤的合約代碼和提交類型
**症狀**：
- 合約代碼顯示為 "TMF" 而不是 "TMFG5"
- 手動操作顯示為 "自動開倉" 而不是 "手動平倉"
- 空單賣出顯示為 "多單買入"

**解決方案**：
1. **已修復**：v1.3.4版本已統一所有失敗通知格式
2. **智能推斷**：訂單回調函數會智能推斷開平倉和手動/自動狀態
3. **合約資訊修復**：改進合約代碼和交割日期的檢索邏輯

### Python語法錯誤修復
**問題**：`name 'contract_txf' is used prior to global declaration` 語法錯誤
**症狀**：系統啟動時出現Python語法錯誤，無法正常運行
**原因**：`global` 宣告位置錯誤，在使用變數之後才宣告
**解決方案**：
1. **已修復**：將 `global` 宣告移到函數開頭
2. **代碼檢查**：確認所有全域變數使用前都有正確的 `global` 宣告

### 錯誤訊息翻譯系統
**新增功能**：`OP_MSG_TRANSLATIONS` 對照表提供友善的中文錯誤訊息
**支援的翻譯**：
- "Order Cancelled" → "手動取消訂單"
- "Insufficient margin" → "保證金不足"
- "Market closed" → "市場已關閉"
- "非該商品可下單時間" → "非交易時間"

## ngrok 相關問題

### 問題：ngrok 顯示 offline 狀態
**症狀**：前端顯示 ngrok 狀態為 offline 或 stopped
**可能原因**：
1. ngrok 進程異常退出
2. 網路連線問題
3. 4040 API 無法訪問
4. 防火牆阻擋

**解決方案**：
1. **檢查自動重啟機制**：系統會自動檢測並在 5 秒後重啟 ngrok
2. **手動重啟**：
   ```bash
   curl -X POST http://localhost:5002/api/ngrok/stop
   sleep 2
   curl -X POST http://localhost:5002/api/ngrok/start
   ```
3. **檢查進程狀態**：
   ```powershell
   Get-Process ngrok
   ```
4. **檢查網路連線**：確認網路穩定，無防火牆阻擋

### 問題：4040 API 無法連線
**症狀**：`Connection refused` 或 `timeout` 錯誤
**可能原因**：
1. ngrok 進程未正常啟動
2. 4040 端口被占用
3. 防火牆阻擋本地連接

**解決方案**：
1. **檢查 ngrok 進程**：
   ```powershell
   Get-Process ngrok
   ```
2. **檢查端口占用**：
   ```powershell
   netstat -an | findstr :4040
   ```
3. **重啟 ngrok**：使用 API 或重啟整個程式
4. **檢查防火牆**：確保本地連接不被阻擋

### 問題：4040 API 返回 500 錯誤
**症狀**：`<Error><StatusCode>500</StatusCode><Message>failed to encode response</Message></Error>`
**原因**：這是已知的 ngrok bug，特別是在較舊版本中
**解決方案**：
1. **自動處理**：程式會靜默處理此錯誤，不影響功能
2. **升級 ngrok**：使用 `/api/ngrok/update` 升級到最新版本
3. **減少請求頻率**：避免過於頻繁的 API 調用

### 問題：ngrok 請求日誌無法顯示
**症狀**：前端 ngrok 請求日誌為空或無法獲取
**可能原因**：
1. 只有通過 ngrok 外部 URL 的請求才會出現在日誌中
2. 本地 localhost 請求不會記錄
3. 4040 API 暫時無法訪問

**解決方案**：
1. **確認外部訪問**：使用 ngrok 提供的公開 URL 訪問
2. **檢查 API 狀態**：
   ```bash
   curl http://localhost:4040/api/requests
   ```
3. **等待自動恢復**：程式會自動處理 API 錯誤

### 問題：ngrok 自動重啟循環
**症狀**：ngrok 不斷重啟，無法穩定運行
**可能原因**：
1. 重啟定時器未正確取消
2. 進程狀態判斷錯誤
3. 網路環境不穩定

**解決方案**：
1. **檢查重啟邏輯**：確認 `ngrok_auto_restart_timer` 正確管理
2. **手動停止重啟**：重啟程式，重置所有定時器
3. **檢查網路**：確保網路環境穩定
4. **查看日誌**：檢查前端系統日誌中的重啟訊息

### 問題：ngrok 版本過舊
**症狀**：功能異常或 API 錯誤頻繁
**解決方案**：
1. **檢查版本**：
   ```bash
   curl http://localhost:5002/api/ngrok/version
   ```
2. **檢查更新**：
   ```bash
   curl -X POST http://localhost:5002/api/ngrok/check_update
   ```
3. **自動升級**：
   ```bash
   curl -X POST http://localhost:5002/api/ngrok/update
   ```

## 交易日判斷問題

### 問題：週六顯示為非交易日
**原因**：舊版本邏輯錯誤
**解決方案**：已修正，週六現在正確顯示為交易日（支援夜盤）

### 問題：假期檔案無法讀取
**症狀**：`讀取假期檔案失敗` 錯誤
**檢查項目**：
1. 檔案是否存在於 `server/holiday/` 目錄
2. 檔案名稱是否為民國年格式（如 `holidaySchedule_114.csv`）
3. 檔案編碼是否為 Big5
4. CSV 格式是否正確

### 問題：交易日判斷不一致
**症狀**：不同頁面顯示不同的交易日狀態
**解決方案**：
1. 確認所有功能都使用 `/api/trading/status` API
2. 檢查是否有地方重複實現交易日判斷邏輯
3. 清除瀏覽器快取

### 問題：民國年轉換錯誤
**症狀**：找不到對應的假期檔案
**檢查項目**：
1. 確認民國年計算：西元年 - 1911
2. 檔案命名格式：`holidaySchedule_XXX.csv`
3. 年份是否正確（如 2025年 = 民國114年）

## 常見系統問題

### 永豐API初始化問題

#### 問題：啟動時出現 'Shioaji' object has no attribute 'on_order_callback' 錯誤
**症狀**：系統啟動時永豐API無法正常登入，出現callback相關錯誤
**原因**：callback設置在API對象完全初始化前就嘗試執行
**解決方案**：
1. **已修復**：v1.3.1版本已將callback設置移動到登入成功後
2. **檢查版本**：確認使用最新版本的代碼
3. **重新啟動**：如仍出現問題，重新啟動應用程式

#### 問題：永豐API連線成功但通知功能異常
**症狀**：API能正常連線和查詢，但下單通知無法正常工作
**原因**：callback設置暫時移除以確保系統穩定性
**說明**：
- 基本交易功能完全正常（查詢、下單、webhook）
- 通知功能改為主動查詢模式，功能完整但略有延遲
- 未來版本將重新實現即時callback功能

#### 問題：shioaji版本兼容性問題
**症狀**：不同版本的shioaji在callback設置上有差異
**解決方案**：
1. **檢查版本**：使用 `/api/sinopac/version` 查看當前版本
2. **更新套件**：使用 `/api/sinopac/auto_update` 自動更新
3. **手動更新**：`pip install --upgrade shioaji`

### TG通知系統問題（v1.3.2修復）

#### 問題：TG通知測試失敗 - module 'shioaji.constant' has no attribute 'FuturesOrderType'
**症狀**：測試TG通知功能時出現 `FuturesOrderType` 屬性錯誤
**原因**：舊代碼使用了不正確的shioaji常數
**解決方案**：
1. **已修復**：v1.3.2版本已參考TXserver.py重構通知系統
2. **重構完成**：改用統一的回調事件處理機制
3. **測試功能移除**：移除易錯的測試功能，直接使用正式功能

#### 問題：TG通知不及時或遺漏
**症狀**：下單後未收到通知或通知延遲
**原因**：舊版本使用主動查詢機制，存在時延和遺漏風險
**解決方案**：
1. **架構重構**：v1.3.2改用即時回調事件處理機制
2. **訂單映射**：使用order_octype_map追踪所有訂單狀態
3. **狀態完整**：支援提交成功、提交失敗、成交等所有狀態通知

#### 問題：TG通知格式不一致
**症狀**：不同狀態的通知格式混亂
**解決方案**：
1. **統一格式化**：實現 `get_formatted_order_message()` 和 `get_formatted_trade_message()`
2. **智能翻譯**：添加 `OP_MSG_TRANSLATIONS` 對照表，提供友善的錯誤訊息
3. **狀態區分**：根據OrderState發送對應的通知類型

### ngrok Token設置問題（v1.3.2新增）

#### 問題：Token無法保存或遺失
**症狀**：設置Token後重啟系統Token消失
**原因**：webview環境下localStorage不持久
**解決方案**：
1. **服務器端存儲**：改用 `server/config/ngrok_token.txt` 檔案存儲
2. **自動創建**：設置時自動創建config目錄
3. **安全讀取**：支援Token格式驗證和安全讀寫

#### 問題：Token格式驗證失敗
**症狀**：輸入Token後提示格式錯誤
**檢查項目**：
1. Token必須以 `1_` 或 `2` 開頭
2. 長度必須大於等於10字符
3. 不包含空格或特殊字符

#### 問題：ngrok設置後無法啟動
**症狀**：Token設置成功但ngrok無法啟動tunnel
**解決方案**：
1. **檢查網路**：確認網路環境正常
2. **驗證Token**：確認Token有效且未過期
3. **查看狀態**：使用 `/api/ngrok/status` 檢查詳細狀態
4. **重新設置**：如有問題可重新設置Token

### webview環境問題（v1.3.2修復）

#### 問題：localStorage數據無法持久保存
**症狀**：重啟應用後所有localStorage數據消失
**原因**：webview的localStorage每次重啟都會清空
**解決方案**：
1. **服務器端存儲**：重要設置改用服務器端檔案存儲
2. **自動載入**：啟動時自動從服務器端載入設置
3. **備份機制**：重要設置提供多重備份方案

### API 連線問題
- 檢查永豐 API 憑證是否過期
- 確認網路連線正常
- 檢查 `.env` 設定檔

### 前端顯示問題
- 清除瀏覽器快取
- 檢查 JavaScript 錯誤
- 確認 API 回應格式正確

## 日誌檢查

### 重要日誌檔案
- `shioaji.log` - 永豐 API 連線日誌
- 瀏覽器開發者工具 - 前端錯誤
- 系統事件日誌 - 應用程式錯誤
- 前端系統日誌 - 通過 `/api/system_log` 記錄

### 關鍵錯誤訊息
- `初始化交易日曆失敗` - 假期檔案問題
- `讀取假期檔案失敗` - 檔案格式或編碼問題
- `API 連線失敗` - 網路或憑證問題
- `ngrok 自動重啟成功` - 自動恢復機制運作
- `ngrok 自動重啟失敗` - 需要手動干預

## 緊急處理流程

### 1. 無法啟動主程式
- 檢查 Python 版本與依賴是否安裝齊全
- 查看終端機錯誤訊息
- 確認 ngrok.exe 存在於正確位置

### 2. API 連線失敗
- 檢查 API Key、憑證、網路連線
- 檢查永豐 API 是否維護中
- 確認 `.env` 設定正確

### 3. ngrok 無法連線
- 檢查網路環境和防火牆設定
- 使用手動重啟 API
- 檢查 ngrok 版本是否需要升級

### 4. Telegram Bot 無法推播
- 檢查 Bot Token 與 Chat ID
- 確認 Bot 是否啟用
- 測試 Bot API 連線

### 5. 系統異常重啟
- 檢查自動重啟機制是否正常
- 查看前端系統日誌
- 確認進程管理是否正確

### 開平倉通知問題（v1.3.3修復）

#### 問題：開平倉類型顯示錯誤
**症狀**：通知中的開平倉類型（自動開倉/自動平倉）顯示不正確
**原因**：
1. 訂單映射中未正確記錄開平倉類型
2. `get_oc_type` 函數邏輯錯誤
3. 通知格式化時未使用正確的開平倉類型

**解決方案**：
1. **已修復**：v1.3.3版本已修正開平倉類型判斷邏輯
2. **檢查版本**：確認使用最新版本的代碼
3. **驗證方式**：
   - 開多/開空應顯示為 "New"
   - 平多/平空應顯示為 "Cover"

#### 問題：訂單映射中缺少開平倉資訊
**症狀**：order_octype_map 中找不到開平倉類型
**解決方案**：
1. **檢查映射建立**：確認下單時正確設置了 octype
2. **更新格式**：使用新版本的訂單映射格式
3. **手動修正**：重新啟動程式，讓系統重建訂單映射

#### 問題：開平倉類型判斷不一致
**症狀**：相同交易方向的訂單顯示不同的開平倉類型
**檢查項目**：
1. 確認所有下單路徑都使用 `get_oc_type('Auto')`
2. 檢查交易方向判斷是否正確
3. 確認通知格式化時使用正確的開平倉類型

#### 問題：開平倉通知格式混亂
**症狀**：不同來源的開平倉通知格式不一致
**解決方案**：
1. **統一格式**：使用 `get_formatted_order_message` 處理所有通知
2. **檢查模板**：確認通知模板包含正確的開平倉類型欄位
3. **更新舊代碼**：移除任何自定義的通知格式化邏輯

---

**重要**：
1. 交易日判斷問題優先檢查 `main.py` 中的邏輯
2. ngrok 問題優先檢查自動重啟機制
3. 4040 API 錯誤是已知問題，程式會自動處理
4. 大部分問題會通過自動恢復機制解決

更多疑難雜症請參考 backup/MAINTENANCE.md 或聯絡開發者。 